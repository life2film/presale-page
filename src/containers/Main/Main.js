import React, {PureComponent} from 'react';

import './Main.css';

export default class Main extends PureComponent {
    render() {
        return (
            <main className="main">
                Main
            </main>
        )
    }
}